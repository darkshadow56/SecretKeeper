<%- include('partials/header') %>
<div class="container mt-5">
  <h1 class="headers">Register</h1>

  <div class="row">
    <div class="col-sm-8">
      <div class="card">
        <div class="card-body">

          <!-- Makes POST request to /register route -->
          <form action="/register" method="POST" id="registerForm">
            <div class="form-group">
              <label for="email" class="labels">Email</label>
              <input type="email" class="form-control" name="username">
            </div>
            <div class="form-group">
              <label for="password" class="labels">Password</label>
              <input type="password" class="form-control" id="password-input" name="password">
            </div>
            <button type="submit" class="btn btn-dark registerPageButton" id="reg-btn">Register</button>
          </form>

        </div>
      </div>
    </div>

    <div class="col-sm-4">
      <div class="card social-block">
        <div class="card-body">
          <a class="btn btn-social btn-google btn-block buttonText" href="/auth/google" role="button">
            <i class="fab fa-google googleButton"></i>
            Sign Up with Google
          </a>
        </div>
      </div>
      <div class="card social-block">
        <div class="card-body">
          <a class="btn btn-social btn-facebook btn-block buttonText" href="/auth/facebook" role="button">
            <i class="fab fa-facebook facebookButton"></i>
            Sign Up with Facebook
          </a>
        </div>
      </div>
    </div>

  </div>
</div>
<script>
  const password = document.getElementById('password-input');
  const registerForm = document.getElementById('registerForm');
  registerForm.addEventListener('submit', (e)=>{
    if(password.value === ""){
    e.preventDefault();
  }
  })
</script>

<%- include('partials/header') %>
